# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage

# Env / secrets. Ignore every .env variant, then un-ignore the template --
# `.env.*` would otherwise swallow .env.example and the one file meant to be
# shared is the one that silently never gets committed.
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
traces*.jsonl
