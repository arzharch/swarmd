{
  "skills": [
    {
      "skill_id": "e623449fffd2",
      "name": "extract_amounts approach",
      "task_pattern": "Extract every date, monetary amount and organisation name from the paragraph below and emit them as a JSON object with one list per category. Paragraph: 'On 14 March 2024 Acme Corporation agreed to pay Beta Industries $2.4 million, with a further $600,000 due by 30 September 2024.'",
      "instruction": "When a step calls for this: Extract all monetary amounts from the paragraph and output them as a list of strings. Produce a JSON object with these fields: amounts (list), dates (list), organisations (list). Values must come from the task at hand -- this records the shape that satisfied the criterion 5 times, not the answer, which will differ.",
      "provenance_run": "run-6aee976840",
      "provenance_criterion": "da4c92e5094fffc2",
      "created_ts": 1788002497.7689452,
      "approved": true,
      "approved_by": "auto-approve",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "0a686abfec76",
      "name": "extract_dates approach",
      "task_pattern": "Extract every date, monetary amount and organisation name from the paragraph below and emit them as a JSON object with one list per category. Paragraph: 'On 14 March 2024 Acme Corporation agreed to pay Beta Industries $2.4 million, with a further $600,000 due by 30 September 2024.'",
      "instruction": "When a step calls for this: Extract all date expressions from the paragraph and output them as a list of strings. Produce a JSON object with these fields: amounts (list), dates (list), organisations (list). Values must come from the task at hand -- this records the shape that satisfied the criterion 5 times, not the answer, which will differ.",
      "provenance_run": "run-6aee976840",
      "provenance_criterion": "da4c92e5094fffc2",
      "created_ts": 1788002498.5244684,
      "approved": true,
      "approved_by": "auto-approve",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "072be9e2f7d8",
      "name": "extract_organisations approach",
      "task_pattern": "Extract every date, monetary amount and organisation name from the paragraph below and emit them as a JSON object with one list per category. Paragraph: 'On 14 March 2024 Acme Corporation agreed to pay Beta Industries $2.4 million, with a further $600,000 due by 30 September 2024.'",
      "instruction": "When a step calls for this: Extract all organisation names from the paragraph and output them as a list of strings. Produce a JSON object with these fields: amounts (list), dates (list), organisations (list). Values must come from the task at hand -- this records the shape that satisfied the criterion 5 times, not the answer, which will differ.",
      "provenance_run": "run-6aee976840",
      "provenance_criterion": "da4c92e5094fffc2",
      "created_ts": 1788002498.5384746,
      "approved": true,
      "approved_by": "auto-approve",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "8ef886ea3554",
      "name": "combine_to_json approach",
      "task_pattern": "Extract every date, monetary amount and organisation name from the paragraph below and emit them as a JSON object with one list per category. Paragraph: 'On 14 March 2024 Acme Corporation agreed to pay Beta Industries $2.4 million, with a further $600,000 due by 30 September 2024.'",
      "instruction": "When a step calls for this: Combine the extracted dates, monetary amounts, and organisations into a single JSON object with one list per category. Produce a JSON object with these fields: amounts (list), dates (list), organisations (list). Values must come from the task at hand -- this records the shape that satisfied the criterion 5 times, not the answer, which will differ.",
      "provenance_run": "run-6aee976840",
      "provenance_criterion": "da4c92e5094fffc2",
      "created_ts": 1788002498.5545018,
      "approved": true,
      "approved_by": "auto-approve",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "ce7573fad38c",
      "name": "calculate_pen_cost approach",
      "task_pattern": "A shop sells pencils at 40 cents and pens at 1.25 dollars. A customer buys 7 pencils and 3 pens and pays with a 20 dollar note. Compute the change, and emit the total, the change, and each line item as structured output.",
      "instruction": "When a step calls for this: Compute the total cost of 3 pens at 1.25 dollars each and produce the monetary amount in dollars. Produce a JSON object with these fields: change (float), pen_total (float), pencil_total (float), total (float). Values must come from the task at hand -- this records the shape that satisfied the criterion 5 times, not the answer, which will differ.",
      "provenance_run": "run-c7b65946dc",
      "provenance_criterion": "28c30f7076d9d265",
      "created_ts": 1788002526.5461931,
      "approved": true,
      "approved_by": "auto-approve",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "e44e61a87596",
      "name": "calculate_pencil_cost approach",
      "task_pattern": "A shop sells pencils at 40 cents and pens at 1.25 dollars. A customer buys 7 pencils and 3 pens and pays with a 20 dollar note. Compute the change, and emit the total, the change, and each line item as structured output.",
      "instruction": "When a step calls for this: Compute the total cost of 7 pencils at 40 cents each and produce the monetary amount in dollars. Produce a JSON object with these fields: change (float), items (list), pen_total (float), pencil_total (float), total (float). Values must come from the task at hand -- this records the shape that satisfied the criterion 4 times, not the answer, which will differ.",
      "provenance_run": "run-c7b65946dc",
      "provenance_criterion": "28c30f7076d9d265",
      "created_ts": 1788002526.6461084,
      "approved": true,
      "approved_by": "auto-approve",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "b59e6ece4939",
      "name": "format_structured_output approach",
      "task_pattern": "A shop sells pencils at 40 cents and pens at 1.25 dollars. A customer buys 7 pencils and 3 pens and pays with a 20 dollar note. Compute the change, and emit the total, the change, and each line item as structured output.",
      "instruction": "When a step calls for this: Emit the line items, total, and change as structured output based on the previous calculations. Produce a JSON object with these fields: change (float), pen_total (float), pencil_total (float), total (float). Values must come from the task at hand -- this records the shape that satisfied the criterion 3 times, not the answer, which will differ.",
      "provenance_run": "run-c7b65946dc",
      "provenance_criterion": "28c30f7076d9d265",
      "created_ts": 1788002526.6627438,
      "approved": true,
      "approved_by": "auto-approve",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "9faf3426794c",
      "name": "extract_file_one_columns approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a list of all column names present in the first CSV file. Produce a JSON object with these fields: file_one_columns (list), file_one_columns_list (list), file_three_columns (list), file_two_columns (list), metadata (str), normalisation_mapping (dict), summary (dict). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2001-t",
      "provenance_criterion": "ff80e8ac3e5b7ee4",
      "created_ts": 1788003367.8930118,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "1a63bf28b894",
      "name": "extract_file_three_columns approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a list of all column names present in the third CSV file. Produce a JSON object with these fields: file_column_counts (dict), file_three_columns (list), normalisation_mapping (dict), summary (str). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2001-t",
      "provenance_criterion": "ff80e8ac3e5b7ee4",
      "created_ts": 1788003367.8960164,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "33629eb487aa",
      "name": "extract_file_two_columns approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a list of all column names present in the second CSV file. Produce a JSON object with these fields: file_two_columns (list), normalisation_mapping (dict). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2001-t",
      "provenance_criterion": "ff80e8ac3e5b7ee4",
      "created_ts": 1788003367.898022,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "0d3b2dffecda",
      "name": "generate_normalisation_mapping approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a dictionary mapping inconsistent variant names (like 'Cust ID', 'customer_id', 'CustomerID') to a single canonical column name. Produce a JSON object with these fields: approach (str), canonical_form (str), canonical_name (str), files_contributing_columns (dict), matched_variants (list), normalisation_mapping (dict), normalised_count_per_file (dict), rationale (str), rules_applied (list), strategy (str), summary (str), summary_text (str), total_columns_after_normalisation (int). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2001-t",
      "provenance_criterion": "ff80e8ac3e5b7ee4",
      "created_ts": 1788003367.9000266,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "28cdaafedb19",
      "name": "compute_contribution_summary approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a final summary report showing how many columns each file contributes after applying the normalisation mapping. Produce a JSON object with these fields: description (str), file_1_contributions (int), file_2_contributions (int), file_3_contributions (int), file_column_counts (dict), normalisation_mapping (dict), total_columns_processed (int). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2001-t",
      "provenance_criterion": "ff80e8ac3e5b7ee4",
      "created_ts": 1788003367.9020247,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "4cadebace0a7",
      "name": "extract_file_one_columns approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a list of column names found in the first CSV file. Produce a JSON object with these fields: canonical_column (str), columns_per_file (dict), columns_per_file_file1 (int), columns_per_file_file2 (int), columns_per_file_file3 (int), extracted_cols (list), file_one_columns (list), global_column_count (int), normalisation_map (dict), normalisation_mapping (dict), per_file_column_counts (dict). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2002-t",
      "provenance_criterion": "2f07696b548dde0a",
      "created_ts": 1788003459.4935422,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "7e7bfe974c86",
      "name": "extract_file_three_columns approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a list of column names found in the third CSV file. Produce a JSON object with these fields: canonical_column (str), columns_per_file (dict), columns_per_file_file1 (int), columns_per_file_file2 (int), columns_per_file_file3 (int), file_three_columns (list), global_column_count (int), normalisation_map (dict), normalisation_mapping (dict), per_file_column_counts (dict). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2002-t",
      "provenance_criterion": "2f07696b548dde0a",
      "created_ts": 1788003459.4969523,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "9cb5e3e096fd",
      "name": "extract_file_two_columns approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a list of column names found in the second CSV file. Produce a JSON object with these fields: canonical_column (str), columns_per_file (dict), columns_per_file_file1 (int), columns_per_file_file2 (int), columns_per_file_file3 (int), extracted_file_two_columns (list), file_two_columns (list), global_column_count (int), normalisation_map (dict), normalisation_mapping (dict), per_file_column_counts (dict). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2002-t",
      "provenance_criterion": "2f07696b548dde0a",
      "created_ts": 1788003459.498958,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "d3754f74d8e4",
      "name": "generate_normalisation_mapping approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a dictionary mapping inconsistent variant column names across all three files to a single canonical name (e.g., 'customer_id'). Produce a JSON object with these fields: canonical_column (str), columns_per_file (dict), columns_per_file_file1 (int), columns_per_file_file2 (int), columns_per_file_file3 (int), global_column_count (int), normalisation_map (dict), normalisation_mapping (dict), per_file_column_counts (dict). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2002-t",
      "provenance_criterion": "2f07696b548dde0a",
      "created_ts": 1788003459.5024824,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "5e7d3558fd9c",
      "name": "summarise_column_contributions approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a report detailing how many columns each file contributes after applying the normalisation mapping. Produce a JSON object with these fields: canonical_column (str), columns_per_file (dict), columns_per_file_file1 (int), columns_per_file_file2 (int), columns_per_file_file3 (int), global_column_count (int), normalisation_map (dict), normalisation_mapping (dict), per_file_column_counts (dict). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2002-t",
      "provenance_criterion": "2f07696b548dde0a",
      "created_ts": 1788003459.5049887,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "ffc2ba1ebedc",
      "name": "extract_file_one_columns approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a list of column names extracted from the first CSV file. Produce a JSON object with these fields: column_counts (dict), file_one_columns (list), normalization_mapping (dict). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2003-t",
      "provenance_criterion": "fab51d3d73b96761",
      "created_ts": 1788003548.757136,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "4f5b12017924",
      "name": "extract_file_three_columns approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a list of column names extracted from the third CSV file. Produce a JSON object with these fields: column_counts (dict), columns (list), normalization_mapping (dict). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2003-t",
      "provenance_criterion": "fab51d3d73b96761",
      "created_ts": 1788003548.7650623,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "386552596f26",
      "name": "extract_file_two_columns approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a list of column names extracted from the second CSV file. Produce a JSON object with these fields: column_counts (dict), extracted_columns (list), normalization_mapping (dict). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2003-t",
      "provenance_criterion": "fab51d3d73b96761",
      "created_ts": 1788003548.7683394,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "595ebfb1a856",
      "name": "generate_normalisation_mapping approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a dictionary mapping inconsistent variants like 'Cust ID' and 'customer_id' to a single canonical column name based on all extracted columns. Produce a JSON object with these fields: column_counts (dict), normalization_mapping (dict). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2003-t",
      "provenance_criterion": "fab51d3d73b96761",
      "created_ts": 1788003548.7708864,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    },
    {
      "skill_id": "3dd355c1bdb3",
      "name": "summarise_file_contributions approach",
      "task_pattern": "A CSV has inconsistent column names ('Cust ID', 'customer_id', 'CustomerID') across three files. Produce a normalisation mapping and a summary of how many columns each file contributes after normalisation.",
      "instruction": "When a step calls for this: Produce a final summary report showing how many columns each file contributes after applying the normalisation mapping. Produce a JSON object with these fields: column_counts (dict), normalization_mapping (dict). Values must come from the task at hand -- this records the shape that satisfied the criterion 2 times, not the answer, which will differ.",
      "provenance_run": "eval-cus-wrangle-1-2003-t",
      "provenance_criterion": "fab51d3d73b96761",
      "created_ts": 1788003548.773884,
      "approved": false,
      "approved_by": "",
      "uses": 0,
      "successes": 0,
      "retired": false,
      "retired_reason": ""
    }
  ]
}